import Signal from 'signals';
const BISignal = {
      tableShortcutSignal: new Signal(),
      settingToolBarSignal:new Signal()
}
export class command{
	 constructor(type,parameters=[]){

	 }
}
export default BISignal
